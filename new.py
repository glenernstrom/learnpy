from random import randint

def coin_flip():
    if randint(0, 1) == 0:
        return "heads"
    else:
        return "tails"

for trial in range (10_000):

    while flip 

      flip_result = coin_flip()
      print(flip_result)


# print (flips / 10_000)
  # while coin_flip() = previous coin flips or switches from
  # one state to another gate metaphor! it interrupts only if
  # there is a change!

  # one state heads, heads, heads, etc.
  # another state tails, tails, tails, etc.
  # the state is established by the first flip
  # we want avg flips_taken per trial - trial loop
  # and flip loop that will repeat as long as condition is true
